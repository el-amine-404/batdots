#!/usr/bin/env bash
source "${DOTFILES_ROOT}/lib/bash-utilities.sh"
source "${DOTFILES_ROOT}/lib/themes.sh"

themes::install_from_list "openbox"

# openbox theme selected = Degrade-Froly (use obconf to apply)
