[Unit]
Description=Nightly restic backup

[Timer]
OnCalendar=*-*-* 02:00:00
# Spread load and avoid a thundering herd if several machines share a remote.
RandomizedDelaySec=30m
# If the machine was off at 02:00, run as soon as it's back on.
Persistent=true

[Install]
WantedBy=timers.target
